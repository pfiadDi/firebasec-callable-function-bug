import { FirebaseOptions } from "firebase/app";

export const firebaseConfig = (host: string): FirebaseOptions => {
  return {
    apiKey: "your project api keys"
    authDomain: "your project api keys"
    projectId: "your project api keys"
    storageBucket: "your project api keys"
    messagingSenderId: "your project api keys"
    appId: "your project api keys"
    measurementId: "your project api keys"
  };
};
